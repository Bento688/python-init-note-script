# Automatic Python Note Initialization

This simple python script integrates very well with the current workflow that I have. it checks in my local machine, whether the notes in the `md-notes` directory is present. and then automatically inits an empty template if not found.
